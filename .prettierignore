package-lock.json
node_modules
dist
flake.lock
# SOPS-encrypted YAML — reformatting would invalidate the MAC.
secrets/
# Generated by `npm run schemas:generate`; CI staleness check (schemas:check)
# enforces the generator output verbatim, so prettier must not rewrite them.
schemas/envelopes/
schemas/packets/
schemas/openapi/
