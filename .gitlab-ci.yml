# Colony CI pipeline.
#
# Colony packages a container image; the aether repo owns every cluster
# object (tofu/home/kubernetes/colony.tf) and Keel force-polls :latest,
# so a green main branch redeploys itself. Image builds run on Aether's
# `gitlab-runner-k8s` (tagged `buildah`).

stages:
  - validate
  - unit
  - build

variables:
  NIX_CONFIG: "experimental-features = nix-command flakes"
  NPM_CONFIG_CACHE: "$CI_PROJECT_DIR/.npm-cache"
  STORAGE_DRIVER: "vfs"
  BUILDAH_FORMAT: "docker"

workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_TAG

default:
  image: nixos/nix:2.21.2
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - .npm-cache/
      - node_modules/

.with-deps:
  before_script:
    - nix --version
    - nix develop --command npm ci --prefer-offline --no-audit --no-fund

validate:
  stage: validate
  extends: .with-deps
  script:
    - nix develop --command bash -c 'set -euxo pipefail; npm run lint && npm run typecheck'

unit-tests:
  stage: unit
  extends: .with-deps
  script:
    - nix develop --command npm run test:unit

build:colonyd:
  stage: build
  image: quay.io/buildah/stable:latest
  tags:
    - buildah
  cache: []
  before_script:
    - buildah --version
    - echo "$CI_REGISTRY_PASSWORD" | buildah login -u "$CI_REGISTRY_USER" --password-stdin "$CI_REGISTRY"
  script:
    - IMAGE="$CI_REGISTRY_IMAGE/colonyd"
    - buildah bud --layers=true --build-arg "COLONY_VERSION=$CI_COMMIT_SHA" -f Dockerfile -t "$IMAGE:$CI_COMMIT_SHA" .
    - buildah push "$IMAGE:$CI_COMMIT_SHA"
    - |
      if [ "$CI_COMMIT_BRANCH" = "$CI_DEFAULT_BRANCH" ]; then
        buildah tag "$IMAGE:$CI_COMMIT_SHA" "$IMAGE:latest"
        buildah push "$IMAGE:latest"
      fi
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
