node_modules/
dist/
build/
*.tsbuildinfo
.env
.env.*
!.env.example
config/colony.yaml
.DS_Store
coverage/
.nix-result
.svelte-kit/
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
*.tfplan.*
plan.cache
.claude/
logs/
.dev-logs/
data/
test-results/
playwright-report/

# Vendored console UI deps are script output (bun run vendor:ui) but committed
# so a fresh clone serves them; VENDOR.md documents the exact versions.
