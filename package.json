{
  "name": "colony",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "bun --watch apps/colonyd/src/main.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test apps packages scripts",
    "test:unit": "bun test apps packages scripts --path-ignore-patterns='**/*.integration.test.ts'",
    "test:e2e": "playwright test",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/bun": "^1.3.14",
    "prettier": "3.8.3",
    "typescript": "^5.7.2"
  }
}
