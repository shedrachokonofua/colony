// Human kind names for run kinds, keyed by the runs.kind column values.
// Ported from the monolith (app.js KIND_LABEL) so element views label kinds
// identically.
export const KIND_LABEL = {
  architect: "plan",
  implement: "build",
  review: "review",
  merge_gate: "gate",
  validate: "validate",
};
